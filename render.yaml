services:
  - type: web
    name: oxyguard-v3
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OXYGUARD_AUTH_EMAIL
        value: robertmarson88@gmail.com
      - key: OXYGUARD_EMAIL_FROM
        value: OxyGuard <onboarding@resend.dev>
      - key: RESEND_API_KEY
        sync: false
